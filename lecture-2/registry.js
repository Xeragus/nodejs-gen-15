const people = require('./people');
const { yaya, hehe, hihi } = require('./people-data-printer');

// printerObj.yaya(people);
// console.log(printerObj.hihi);

yaya(people)
hehe()
console.log(hihi);